<h2>User info</h2>
    <div class = "row">
        <div class = "col-xs-3"
            <%= render 'info', user: current_user %>
            <h2 class="mt-3">New book</h2>
            <%= render 'books/form', books: @book %>
        </div>
        <div class = "col-xs-5" >
            <h2>Users</h2>
            <%= render 'books/index', users: @users %>
        </div>
    </div>